@{
ViewData["Title"] = "HockeyDJ Setup";
}

<!DOCTYPE html>
<html>

<head>
    <title>@ViewData["Title"]</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
            color: white;
            margin: 0;
            padding: 20px;
            min-height: 100vh;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 40px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
        }

        .header h1 {
            font-size: 2.5em;
            margin: 0;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            background: linear-gradient(45deg, #ff6b35, #f7931e);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #ff6b35;
        }

        .form-control {
            width: 100%;
            padding: 12px;
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 8px;
            background: rgba(255, 255, 255, 0.1);
            color: white;
            font-size: 16px;
            box-sizing: border-box;
        }

        .form-control:focus {
            outline: none;
            border-color: #ff6b35;
            box-shadow: 0 0 10px rgba(255, 107, 53, 0.3);
        }

        .form-control::placeholder {
            color: rgba(255, 255, 255, 0.6);
        }

        textarea.form-control {
            height: 120px;
            resize: vertical;
        }

        .btn {
            background: linear-gradient(45deg, #28a745, #20c997);
            color: white;
            padding: 15px 30px;
            border: none;
            border-radius: 8px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            width: 100%;
            transition: all 0.3s ease;
        }

        .btn:hover {
            background: linear-gradient(45deg, #218838, #1ba085);
            transform: translateY(-2px);
        }

        .btn-secondary {
            background: linear-gradient(45deg, #6c757d, #5a6268);
            margin-bottom: 10px;
        }

        .btn-secondary:hover {
            background: linear-gradient(45deg, #545b62, #4e555b);
        }

        .btn-warning {
            background: linear-gradient(45deg, #ffc107, #e0a800);
            color: #212529;
        }

        .btn-warning:hover {
            background: linear-gradient(45deg, #e0a800, #d39e00);
        }

        .btn-group {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
            margin-bottom: 20px;
        }

        .instructions {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 30px;
            border-left: 4px solid #ff6b35;
        }

        .instructions h3 {
            margin-top: 0;
            color: #ff6b35;
        }

        .instructions ol {
            padding-left: 20px;
        }

        .instructions li {
            margin-bottom: 10px;
            line-height: 1.5;
        }

        .error {
            background: rgba(220, 53, 69, 0.2);
            border: 1px solid #dc3545;
            border-radius: 8px;
            padding: 15px;
            margin: 20px 0;
            text-align: center;
        }

        .success {
            background: rgba(40, 167, 69, 0.2);
            border: 1px solid #28a745;
            border-radius: 8px;
            padding: 15px;
            margin: 20px 0;
            text-align: center;
            color: #d4edda;
        }

        .code {
            background: rgba(0, 0, 0, 0.3);
            padding: 4px 8px;
            border-radius: 4px;
            font-family: 'Courier New', monospace;
            font-size: 14px;
        }

        .goal-horn-section {
            background: rgba(255, 107, 53, 0.1);
            border: 1px solid rgba(255, 107, 53, 0.3);
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 25px;
        }

        .goal-horn-section h4 {
            color: #ff6b35;
            margin-top: 0;
            font-size: 1.3em;
        }

        .config-tools {
            background: rgba(138, 43, 226, 0.1);
            border: 1px solid rgba(138, 43, 226, 0.3);
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 25px;
        }

        .config-tools h4 {
            color: #ba55d3;
            margin-top: 0;
            font-size: 1.3em;
        }

        #importFile {
            display: none;
        }

        .file-input-wrapper {
            position: relative;
            overflow: hidden;
            display: inline-block;
            width: 100%;
        }

        .file-input-wrapper input[type=file] {
            position: absolute;
            left: -9999px;
        }

        .file-input-wrapper .file-input-button {
            background: linear-gradient(45deg, #17a2b8, #138496);
            color: white;
            padding: 15px 30px;
            border: none;
            border-radius: 8px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            width: 100%;
            transition: all 0.3s ease;
            display: block;
            text-align: center;
        }

        .file-input-wrapper .file-input-button:hover {
            background: linear-gradient(45deg, #138496, #0f6674);
            transform: translateY(-2px);
        }

        .selected-file {
            margin-top: 10px;
            padding: 10px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 5px;
            font-size: 14px;
            color: #b8daff;
        }

        .divider {
            border-top: 2px solid rgba(255, 255, 255, 0.2);
            margin: 30px 0;
            position: relative;
        }

        .divider::after {
            content: "OR";
            position: absolute;
            top: -12px;
            left: 50%;
            transform: translateX(-50%);
            background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
            padding: 0 15px;
            color: rgba(255, 255, 255, 0.7);
            font-weight: bold;
        }

        /* Stats Mode Toggle Styles */
        .stats-toggle-section {
            background: rgba(40, 167, 69, 0.1);
            border: 2px solid rgba(40, 167, 69, 0.3);
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 25px;
        }

        .stats-toggle-section h4 {
            color: #28a745;
            margin-top: 0;
            font-size: 1.3em;
        }

        .toggle-container {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .toggle-switch {
            position: relative;
            width: 60px;
            height: 30px;
        }

        .toggle-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

        .toggle-slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: rgba(255, 255, 255, 0.2);
            transition: 0.4s;
            border-radius: 30px;
        }

        .toggle-slider:before {
            position: absolute;
            content: "";
            height: 22px;
            width: 22px;
            left: 4px;
            bottom: 4px;
            background-color: white;
            transition: 0.4s;
            border-radius: 50%;
        }

        input:checked + .toggle-slider {
            background-color: #28a745;
        }

        input:checked + .toggle-slider:before {
            transform: translateX(30px);
        }

        .toggle-label {
            font-size: 16px;
            color: white;
        }

        /* Stats Mode Section Styles */
        .stats-mode-section {
            background: rgba(40, 167, 69, 0.1);
            border: 1px solid rgba(40, 167, 69, 0.3);
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 25px;
            display: none;
        }

        .stats-mode-section.visible {
            display: block;
        }

        .stats-mode-section h4 {
            color: #28a745;
            margin-top: 0;
            font-size: 1.3em;
        }

        .playlist-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
        }

        .playlist-grid .form-group {
            margin-bottom: 15px;
        }

        .time-settings-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 15px;
        }

        .time-settings-grid .form-group {
            margin-bottom: 15px;
        }

        @@media (max-width: 768px) {
            .playlist-grid, .time-settings-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>🏒 HockeyDJ Setup</h1>
            <p>Configure your Spotify connection and playlists</p>
        </div>

        @if (!string.IsNullOrEmpty(ViewBag.Error))
        {
        <div class="error">@ViewBag.Error</div>
        }

        @if (!string.IsNullOrEmpty(ViewBag.Success))
        {
        <div class="success">@ViewBag.Success</div>
        }

        <div class="config-tools">
            <h4>🔧 Configuration Tools</h4>
            <div class="btn-group">
                <button type="button" class="btn btn-warning" onclick="exportConfiguration()">
                    📤 Export Configuration
                </button>
                <div class="file-input-wrapper">
                    <input type="file" id="importFile" accept=".json" onchange="importConfiguration(this)" />
                    <label for="importFile" class="file-input-button">
                        📥 Import Configuration
                    </label>
                </div>
            </div>
            <div id="selectedFileDisplay" class="selected-file" style="display: none;"></div>
            <p style="font-size: 14px; color: rgba(255,255,255,0.7); margin: 10px 0 0 0;">
                💡 Save your configuration to easily restore settings or share with other devices.
            </p>
        </div>

        <div class="divider"></div>

        <div class="instructions">
            <h3>Setup Instructions</h3>
            <ol>
                <li>Go to <a href="https://developer.spotify.com/dashboard" target="_blank"
                        style="color: #ff6b35;">Spotify Developer Dashboard</a></li>
                <li>Create a new app or use an existing one</li>
                <li>Add your redirect URI to the app settings (e.g., <span
                        class="code">https://localhost:7001/Home/SpotifyCallback</span>)</li>
                <li>Copy your Client ID and Client Secret</li>
                <li>
                    Add your sound files to <span class="code">wwwroot/audio/</span>:
                    <ul style="margin-top: 5px;">
                        <li><span class="code">goal-horn.mp3</span> - Goal celebration sound</li>
                        <li><span class="code">mushroom.mp3</span> - Even Strength (Extra Life) sound</li>
                        <li><span class="code">clock.mp3</span> - One Minute Remaining Sound</li>
                    </ul>
                </li>
                <li>Paste your Spotify playlist URLs below</li>
                <li>Make sure you have Spotify Premium (required for playback control)</li>
            </ol>
        </div>

        <form method="post" action="/Home/SaveSpotifyConfig" id="configForm">
            @Html.AntiForgeryToken()
            <div class="form-group">
                <label for="clientId">Spotify Client ID</label>
                <input type="text" id="clientId" name="clientId" class="form-control"
                    placeholder="Your Spotify App Client ID" required />
            </div>

            <div class="form-group">
                <label for="clientSecret">Spotify Client Secret</label>
                <input type="text" id="clientSecret" name="clientSecret" class="form-control"
                    placeholder="Your Spotify App Client Secret" required />
            </div>

            <div class="form-group">
                <label for="redirectUri">Redirect URI</label>
                <input type="url" id="redirectUri" name="redirectUri" class="form-control"
                    placeholder="https://127.0.0.1:7001/Home/SpotifyCallback"
                    value="https://127.0.0.1:7001/Home/SpotifyCallback" required />
            </div>

            <!-- Stats Tracking Mode Toggle -->
            <div class="stats-toggle-section">
                <h4>📊 Stats Tracking Mode</h4>
                <div class="toggle-container">
                    <label class="toggle-switch">
                        <input type="checkbox" id="statsMode" name="statsMode" onchange="toggleStatsMode()">
                        <span class="toggle-slider"></span>
                    </label>
                    <span class="toggle-label">Enable Stats Tracking Mode</span>
                </div>
                <small style="color: rgba(255,255,255,0.7); font-size: 14px; margin-top: 10px; display: block;">
                    📈 When enabled, HockeyDJ will provide a scoreboard interface with game clock, goal/penalty tracking, and Gamesheet-compatible exports.
                </small>
            </div>

            <!-- Stats Mode Settings (shown only when stats mode is enabled) -->
            <div class="stats-mode-section" id="statsModeSection">
                <h4>⏱️ Game Time Settings</h4>
                <div class="time-settings-grid">
                    <div class="form-group">
                        <label for="periodLength">Period Length (minutes)</label>
                        <input type="number" id="periodLength" name="periodLength" class="form-control"
                            placeholder="15" value="15" min="1" max="30" />
                    </div>
                    <div class="form-group">
                        <label for="warmupLength">Warm-up Length (minutes)</label>
                        <input type="number" id="warmupLength" name="warmupLength" class="form-control"
                            placeholder="10" value="10" min="1" max="30" />
                    </div>
                    <div class="form-group">
                        <label for="intermissionLength">Intermission Length (minutes)</label>
                        <input type="number" id="intermissionLength" name="intermissionLength" class="form-control"
                            placeholder="15" value="15" min="1" max="30" />
                    </div>
                </div>

                <h4 style="margin-top: 25px;">🎵 Situation Playlists</h4>
                <small style="color: rgba(255,255,255,0.7); font-size: 14px; margin-bottom: 15px; display: block;">
                    Configure playlists for different game situations. These will play automatically based on game events.
                </small>
                <div class="playlist-grid">
                    <div class="form-group">
                        <label for="awayGoalPlaylist">Away Goal Playlist</label>
                        <input type="url" id="awayGoalPlaylist" name="awayGoalPlaylist" class="form-control"
                            placeholder="https://open.spotify.com/playlist/..." />
                    </div>
                    <div class="form-group">
                        <label for="offsidePlaylist">Offside Playlist</label>
                        <input type="url" id="offsidePlaylist" name="offsidePlaylist" class="form-control"
                            placeholder="https://open.spotify.com/playlist/..." />
                    </div>
                    <div class="form-group">
                        <label for="icingPlaylist">Icing Playlist</label>
                        <input type="url" id="icingPlaylist" name="icingPlaylist" class="form-control"
                            placeholder="https://open.spotify.com/playlist/..." />
                    </div>
                    <div class="form-group">
                        <label for="comebackPlaylist">Comeback Playlist</label>
                        <input type="url" id="comebackPlaylist" name="comebackPlaylist" class="form-control"
                            placeholder="https://open.spotify.com/playlist/..." />
                    </div>
                    <div class="form-group">
                        <label for="hopePlaylist">Hope Playlist</label>
                        <input type="url" id="hopePlaylist" name="hopePlaylist" class="form-control"
                            placeholder="https://open.spotify.com/playlist/..." />
                    </div>
                    <div class="form-group">
                        <label for="endZonePlaylist">End Zone Playlist</label>
                        <input type="url" id="endZonePlaylist" name="endZonePlaylist" class="form-control"
                            placeholder="https://open.spotify.com/playlist/..." />
                    </div>
                    <div class="form-group">
                        <label for="penaltyPlaylist">Penalty Playlist</label>
                        <input type="url" id="penaltyPlaylist" name="penaltyPlaylist" class="form-control"
                            placeholder="https://open.spotify.com/playlist/..." />
                    </div>
                    <div class="form-group">
                        <label for="warmupPlaylist">Warm-up Playlist</label>
                        <input type="url" id="warmupPlaylist" name="warmupPlaylist" class="form-control"
                            placeholder="https://open.spotify.com/playlist/..." />
                    </div>
                    <div class="form-group">
                        <label for="intermissionPlaylist">Intermission Playlist</label>
                        <input type="url" id="intermissionPlaylist" name="intermissionPlaylist" class="form-control"
                            placeholder="https://open.spotify.com/playlist/..." />
                    </div>
                    <div class="form-group">
                        <label for="postGamePlaylist">Post-Game Playlist</label>
                        <input type="url" id="postGamePlaylist" name="postGamePlaylist" class="form-control"
                            placeholder="https://open.spotify.com/playlist/..." />
                    </div>
                </div>
            </div>

            <div class="goal-horn-section">
                <h4>🚨 Goal Horn Configuration 🚨</h4>
                <div class="form-group">
                    <label for="goalHornPlaylist">Goal Celebration Playlist URL</label>
                    <input type="url" id="goalHornPlaylist" name="goalHornPlaylist" class="form-control"
                        placeholder="https://open.spotify.com/playlist/your-goal-celebration-playlist" />
                    <small style="color: rgba(255,255,255,0.7); font-size: 14px; margin-top: 5px; display: block;">
                        🎵 This playlist will play victory songs after the goal horn sound. Leave empty to use the first
                        playlist below.
                    </small>
                </div>
                <div class="form-group">
                    <label for="songStartTimestamps">Song Start Timestamps (Player Number:Timestamp, one per line)</label>
                    <textarea id="songStartTimestamps" name="songStartTimestamps" class="form-control"
                        placeholder="4:0:45&#10;7:1:30&#10;9:0:15"
                        style="height: 120px;"></textarea>
                    <small style="color: rgba(255,255,255,0.7); font-size: 14px; margin-top: 5px; display: block;">
                        ⏱️ Format: PlayerNumber:Minutes:Seconds (e.g., "4:1:30" means player 4's song starts at 1 minute 30 seconds).<br>
                        When you select a player number before triggering the goal horn, their song will start at this timestamp. Leave blank to start songs from the beginning.
                    </small>
                </div>
            </div>

            <div class="goal-horn-section">
                <h4>🏒 Team Roster Configuration 🏒</h4>
                <div class="form-group">
                    <label for="homeTeamName">Home Team Name</label>
                    <input type="text" id="homeTeamName" name="homeTeamName" class="form-control"
                        placeholder="e.g., Lightning, Maple Leafs" />
                </div>
                <div class="form-group">
                    <label for="homeTeamRoster">Home Team Roster (Number:Name, one per line)</label>
                    <textarea id="homeTeamRoster" name="homeTeamRoster" class="form-control"
                        placeholder="1:John Smith&#10;4:Mike Johnson&#10;7:Chris Williams&#10;8:Dave Brown&#10;9:Tom Davis"
                        style="height: 150px;"></textarea>
                    <small style="color: rgba(255,255,255,0.7); font-size: 14px; margin-top: 5px; display: block;">
                        📝 Format: Number:Name (e.g., "7:Connor McDavid")
                    </small>
                </div>
                <div class="form-group">
                    <label for="awayTeamName">Away Team Name</label>
                    <input type="text" id="awayTeamName" name="awayTeamName" class="form-control"
                        placeholder="e.g., Bruins, Rangers" />
                </div>
                <div class="form-group">
                    <label for="awayTeamRoster">Away Team Roster (Number:Name, one per line)</label>
                    <textarea id="awayTeamRoster" name="awayTeamRoster" class="form-control"
                        placeholder="10:Alex Parker&#10;15:Brad Wilson&#10;16:Steve Miller&#10;17:Rob Taylor&#10;18:Joe Anderson"
                        style="height: 150px;"></textarea>
                    <small style="color: rgba(255,255,255,0.7); font-size: 14px; margin-top: 5px; display: block;">
                        📝 Format: Number:Name (e.g., "97:Connor McDavid")
                    </small>
                </div>
            </div>

            <div class="form-group">
                <label for="playlistUrls">Spotify Playlist URLs (up to 10, one per line)</label>
                <textarea id="playlistUrls" name="playlistUrls" class="form-control"
                    placeholder="https://open.spotify.com/playlist/20ObIj4drcpqU4a4ZQPWNU?si=Y8kcaUGxRD6eov5JXfqzmQ&pi=DoFqrvE4RQm6q&#10;https://open.spotify.com/playlist/0Koymm18OCvJcecMXx7TWp?si=ofxcgkaDR8yYRw0wyhQMzw&pi=Hr6_KlG9QbmU0"
                    required></textarea>
                <small style="color: rgba(255,255,255,0.7); font-size: 14px;">
                    Tip: Right-click any Spotify playlist → "Share" → "Copy link" to get the URL
                </small>
            </div>

            <button type="submit" class="btn">Connect to Spotify & Start HockeyDJ</button>
        </form>
    </div>

    <script>
        // Stats mode toggle function
        function toggleStatsMode() {
            const statsMode = document.getElementById('statsMode').checked;
            const statsModeSection = document.getElementById('statsModeSection');
            if (statsMode) {
                statsModeSection.classList.add('visible');
            } else {
                statsModeSection.classList.remove('visible');
            }
        }

        function exportConfiguration() {
            // Gather all configuration data
            const config = {
                clientId: document.getElementById('clientId').value,
                clientSecret: document.getElementById('clientSecret').value,
                redirectUri: document.getElementById('redirectUri').value,
                goalHornPlaylist: document.getElementById('goalHornPlaylist').value,
                playlistUrls: document.getElementById('playlistUrls').value,
                homeTeamName: document.getElementById('homeTeamName').value,
                homeTeamRoster: document.getElementById('homeTeamRoster').value,
                awayTeamName: document.getElementById('awayTeamName').value,
                awayTeamRoster: document.getElementById('awayTeamRoster').value,
                songStartTimestamps: document.getElementById('songStartTimestamps').value,
                // Stats mode settings
                statsMode: document.getElementById('statsMode').checked,
                periodLength: document.getElementById('periodLength').value,
                warmupLength: document.getElementById('warmupLength').value,
                intermissionLength: document.getElementById('intermissionLength').value,
                awayGoalPlaylist: document.getElementById('awayGoalPlaylist').value,
                offsidePlaylist: document.getElementById('offsidePlaylist').value,
                icingPlaylist: document.getElementById('icingPlaylist').value,
                comebackPlaylist: document.getElementById('comebackPlaylist').value,
                hopePlaylist: document.getElementById('hopePlaylist').value,
                endZonePlaylist: document.getElementById('endZonePlaylist').value,
                penaltyPlaylist: document.getElementById('penaltyPlaylist').value,
                warmupPlaylist: document.getElementById('warmupPlaylist').value,
                intermissionPlaylist: document.getElementById('intermissionPlaylist').value,
                postGamePlaylist: document.getElementById('postGamePlaylist').value,
                exportDate: new Date().toISOString(),
                version: "1.4.0"
            };

            // Create and download the file
            const dataStr = JSON.stringify(config, null, 2);
            const dataUri = 'data:application/json;charset=utf-8,' + encodeURIComponent(dataStr);

            const exportFileDefaultName = 'hockeydj-config-' + new Date().toISOString().split('T')[0] + '.json';

            const linkElement = document.createElement('a');
            linkElement.setAttribute('href', dataUri);
            linkElement.setAttribute('download', exportFileDefaultName);
            linkElement.click();
            linkElement.remove();
        }

        function importConfiguration(input) {
            const file = input.files[0];
            if (!file) return;

            // Display selected file name
            const fileDisplay = document.getElementById('selectedFileDisplay');
            fileDisplay.textContent = `Selected: ${file.name}`;
            fileDisplay.style.display = 'block';

            const reader = new FileReader();
            reader.onload = function (e) {
                try {
                    const configData = e.target.result;

                    // Send to server for processing and validation
                    fetch('/Home/ImportConfiguration', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                            'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]')?.value || ''
                        },
                        body: JSON.stringify({configData: configData})
                    })
                        .then(response => response.json())
                        .then(result => {
                            if (result.success) {
                                // Populate the form fields with validated data
                                const config = result.config;

                                if (config.clientId) {
                                    document.getElementById('clientId').value = config.clientId;
                                }
                                if (config.clientSecret) {
                                    document.getElementById('clientSecret').value  = config.clientSecret;
                                }
                                if (config.redirectUri) {
                                    document.getElementById('redirectUri').value = config.redirectUri;
                                }
                                if (config.goalHornPlaylist) {
                                    document.getElementById('goalHornPlaylist').value = config.goalHornPlaylist;
                                }
                                if (config.playlistUrls) {
                                    document.getElementById('playlistUrls').value = config.playlistUrls;
                                }
                                if (config.homeTeamName) {
                                    document.getElementById('homeTeamName').value = config.homeTeamName;
                                }
                                if (config.homeTeamRoster) {
                                    document.getElementById('homeTeamRoster').value = config.homeTeamRoster;
                                }
                                if (config.awayTeamName) {
                                    document.getElementById('awayTeamName').value = config.awayTeamName;
                                }
                                if (config.awayTeamRoster) {
                                    document.getElementById('awayTeamRoster').value = config.awayTeamRoster;
                                }
                                if (config.songStartTimestamps) {
                                    document.getElementById('songStartTimestamps').value = config.songStartTimestamps;
                                }
                                // Import stats mode settings
                                if (config.statsMode !== undefined) {
                                    document.getElementById('statsMode').checked = config.statsMode;
                                    toggleStatsMode();
                                }
                                if (config.periodLength) {
                                    document.getElementById('periodLength').value = config.periodLength;
                                }
                                if (config.warmupLength) {
                                    document.getElementById('warmupLength').value = config.warmupLength;
                                }
                                if (config.intermissionLength) {
                                    document.getElementById('intermissionLength').value = config.intermissionLength;
                                }
                                if (config.awayGoalPlaylist) {
                                    document.getElementById('awayGoalPlaylist').value = config.awayGoalPlaylist;
                                }
                                if (config.offsidePlaylist) {
                                    document.getElementById('offsidePlaylist').value = config.offsidePlaylist;
                                }
                                if (config.icingPlaylist) {
                                    document.getElementById('icingPlaylist').value = config.icingPlaylist;
                                }
                                if (config.comebackPlaylist) {
                                    document.getElementById('comebackPlaylist').value = config.comebackPlaylist;
                                }
                                if (config.hopePlaylist) {
                                    document.getElementById('hopePlaylist').value = config.hopePlaylist;
                                }
                                if (config.endZonePlaylist) {
                                    document.getElementById('endZonePlaylist').value = config.endZonePlaylist;
                                }
                                if (config.penaltyPlaylist) {
                                    document.getElementById('penaltyPlaylist').value = config.penaltyPlaylist;
                                }
                                if (config.warmupPlaylist) {
                                    document.getElementById('warmupPlaylist').value = config.warmupPlaylist;
                                }
                                if (config.intermissionPlaylist) {
                                    document.getElementById('intermissionPlaylist').value = config.intermissionPlaylist;
                                }
                                if (config.postGamePlaylist) {
                                    document.getElementById('postGamePlaylist').value = config.postGamePlaylist;
                                }

                                // Show success message
                                showTemporaryMessage('✅ Configuration imported successfully! Please enter your Client Secret to complete the setup.', 'success');

                                // Focus on client secret field since it's not exported for security
                                document.getElementById('clientSecret').focus();
                            } else {
                                throw new Error(result.error || 'Unknown error occurred');
                            }
                        })
                        .catch(error => {
                            showTemporaryMessage('❌ Error importing configuration: ' + error.message, 'error');
                        })
                        .finally(() => {
                            // Clear the file input
                            input.value = '';
                            fileDisplay.style.display = 'none';
                        });

                } catch (error) {
                    showTemporaryMessage('❌ Error reading configuration file: ' + error.message, 'error');
                    input.value = '';
                    fileDisplay.style.display = 'none';
                }
            };
            reader.readAsText(file);
        }

        function showTemporaryMessage(message, type) {
            // Remove existing temporary messages
            const existingMessages = document.querySelectorAll('.temp-message');
            existingMessages.forEach(msg => msg.remove());

            // Create new message element
            const messageDiv = document.createElement('div');
            messageDiv.className = `${type} temp-message`;
            messageDiv.textContent = message;

            // Insert after the header
            const header = document.querySelector('.header');
            header.insertAdjacentElement('afterend', messageDiv);

            // Remove after 5 seconds
            setTimeout(() => {
                messageDiv.remove();
            }, 5000);
        }

        // Enhance form validation
        document.getElementById('configForm').addEventListener('submit', function (e) {
            const clientId = document.getElementById('clientId').value.trim();
            const clientSecret = document.getElementById('clientSecret').value.trim();
            const redirectUri = document.getElementById('redirectUri').value.trim();
            const playlistUrls = document.getElementById('playlistUrls').value.trim();

            if (!clientId || !clientSecret || !redirectUri || !playlistUrls) {
                e.preventDefault();
                showTemporaryMessage('❌ Please fill in all required fields!', 'error');
                return false;
            }

            // Validate redirect URI format
            try {
                new URL(redirectUri);
            } catch {
                e.preventDefault();
                showTemporaryMessage('❌ Please enter a valid redirect URI!', 'error');
                return false;
            }

            // Validate playlist URLs
            const urls = playlistUrls.split('\n').filter(url => url.trim());
            const invalidUrls = urls.filter(url => {
                try {
                    const urlObj = new URL(url.trim());
                    return !urlObj.hostname.includes('spotify.com');
                } catch {
                    return true;
                }
            });

            if (invalidUrls.length > 0) {
                e.preventDefault();
                showTemporaryMessage('❌ Please ensure all playlist URLs are valid Spotify URLs!', 'error');
                return false;
            }

            if (urls.length > 10) {
                e.preventDefault();
                showTemporaryMessage('❌ Maximum of 10 playlists allowed!', 'error');
                return false;
            }
        });

        // Populate existing roster data if available
        window.addEventListener('DOMContentLoaded', function() {
            const homeTeamName = '@ViewBag.HomeTeamName';
            const homeTeamRoster = '@Html.Raw(ViewBag.HomeTeamRoster ?? "")';
            const awayTeamName = '@ViewBag.AwayTeamName';
            const awayTeamRoster = '@Html.Raw(ViewBag.AwayTeamRoster ?? "")';
            const songStartTimestamps = '@Html.Raw(ViewBag.SongStartTimestamps ?? "")';
            const statsMode = '@ViewBag.StatsMode' === 'True';

            if (homeTeamName) {
                document.getElementById('homeTeamName').value = homeTeamName;
            }
            if (homeTeamRoster) {
                document.getElementById('homeTeamRoster').value = homeTeamRoster;
            }
            if (awayTeamName) {
                document.getElementById('awayTeamName').value = awayTeamName;
            }
            if (awayTeamRoster) {
                document.getElementById('awayTeamRoster').value = awayTeamRoster;
            }
            if (songStartTimestamps) {
                document.getElementById('songStartTimestamps').value = songStartTimestamps;
            }
            // Load stats mode settings
            if (statsMode) {
                document.getElementById('statsMode').checked = true;
                toggleStatsMode();
            }
            const periodLength = '@ViewBag.PeriodLength';
            const warmupLength = '@ViewBag.WarmupLength';
            const intermissionLength = '@ViewBag.IntermissionLength';
            if (periodLength && periodLength !== '0') {
                document.getElementById('periodLength').value = periodLength;
            }
            if (warmupLength && warmupLength !== '0') {
                document.getElementById('warmupLength').value = warmupLength;
            }
            if (intermissionLength && intermissionLength !== '0') {
                document.getElementById('intermissionLength').value = intermissionLength;
            }
            // Load situation playlists
            const awayGoalPlaylist = '@ViewBag.AwayGoalPlaylist';
            const offsidePlaylist = '@ViewBag.OffsidePlaylist';
            const icingPlaylist = '@ViewBag.IcingPlaylist';
            const comebackPlaylist = '@ViewBag.ComebackPlaylist';
            const hopePlaylist = '@ViewBag.HopePlaylist';
            const endZonePlaylist = '@ViewBag.EndZonePlaylist';
            const penaltyPlaylist = '@ViewBag.PenaltyPlaylist';
            const warmupPlaylist = '@ViewBag.WarmupPlaylist';
            const intermissionPlaylist = '@ViewBag.IntermissionPlaylist';
            const postGamePlaylist = '@ViewBag.PostGamePlaylist';
            
            if (awayGoalPlaylist) document.getElementById('awayGoalPlaylist').value = awayGoalPlaylist;
            if (offsidePlaylist) document.getElementById('offsidePlaylist').value = offsidePlaylist;
            if (icingPlaylist) document.getElementById('icingPlaylist').value = icingPlaylist;
            if (comebackPlaylist) document.getElementById('comebackPlaylist').value = comebackPlaylist;
            if (hopePlaylist) document.getElementById('hopePlaylist').value = hopePlaylist;
            if (endZonePlaylist) document.getElementById('endZonePlaylist').value = endZonePlaylist;
            if (penaltyPlaylist) document.getElementById('penaltyPlaylist').value = penaltyPlaylist;
            if (warmupPlaylist) document.getElementById('warmupPlaylist').value = warmupPlaylist;
            if (intermissionPlaylist) document.getElementById('intermissionPlaylist').value = intermissionPlaylist;
            if (postGamePlaylist) document.getElementById('postGamePlaylist').value = postGamePlaylist;
        });
    </script>
</body>

</html>
